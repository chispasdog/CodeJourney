
/*La función AVG() en Oracle es una función de agregación que se utiliza para calcular el promedio de un conjunto de valores numéricos en una tabla. Esta función ignora los valores no numéricos y las filas con valores nulos.
*/
SELECT AVG(ventas_diarias)
FROM ventas;
